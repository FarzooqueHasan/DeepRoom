rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DeepRoom Security Rules
    // Allows all authenticated users (Email, Google, Anonymous) full access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
